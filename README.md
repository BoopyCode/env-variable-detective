# ENV Variable Detective

Finds all .env files and checks which variables are actually set in your environment. Run: python env_detective.py
Returns exit code 1 if missing variables are found, 0 if all present.